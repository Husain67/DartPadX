# DartMini IDE

A beautiful mobile-first IDE for compiling Dart code.

## Requirements

Run this app using the `--dart-define` flag to provide the required OneCompiler API key:

```bash
flutter run --dart-define=OC_API_KEY=YOUR_API_KEY
```
