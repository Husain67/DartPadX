import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts test', (WidgetTester tester) async {
    // Basic test to ensure test runner passes
    expect(true, isTrue);
  });
}
