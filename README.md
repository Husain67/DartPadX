# DartMini IDE

A mobile-first Flutter code editor with custom execution preset capabilities.

## Setup

To run this app properly, you must provide the OneCompiler API key via a Dart define parameter.

```bash
flutter run --dart-define=ONECOMPILER_KEY=your_actual_key_here
```
